<%= form_with(model: caw, local: true) do |form| %>
  <% if caw.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(caw.errors.count, "error") %> prohibited this caw from being saved:</h2>

      <ul>
      <% caw.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_field :message, id: :caw_message %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :caw_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
